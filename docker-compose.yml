services:
  app:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
    volumes:
      - ./frontend:/app
      - /app/node_modules
    # .env.local frontend/ içinde olmalı; volume ile mount edilir, Vite okur.
